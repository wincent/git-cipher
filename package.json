{
  "name": "git-cipher",
  "version": "2.0.0-pre.4",
  "bin": "./lib/main.mjs",
  "files": [
    "docs",
    "lib"
  ],
  "description": "Manages encrypted content in a Git repo",
  "exports": null,
  "repository": "https://github.com/wincent/git-cipher",
  "author": "Greg Hurrell <greg@hurrell.net>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.1.0",
    "esbuild": "^0.25.8",
    "prettier": "^3.6.2",
    "typescript": "5.9.2"
  },
  "scripts": {
    "build": "esbuild src/main.mts --bundle --format=esm --platform=node --sourcemap --outfile=lib/main.mjs",
    "clean": "rm -r lib/*",
    "compile": "tsc --noEmit",
    "format:check": "prettier --check \"**/*.{md,mts}\" \"*.{md,json}\"",
    "format": "prettier --write \"**/*.{md,mts}\" \"*.{md,json}\""
  },
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
